/**
 * Forms use TanStack Form. Re-exported here so apps import from one place and
 * pair it with the shared Input/Label/Button primitives + InlineError.
 */
export { useForm, useStore } from '@tanstack/react-form';
export type { AnyFieldApi } from '@tanstack/react-form';
